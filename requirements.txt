httpx>=0.27.0
pyyaml>=6.0.1
Pillow>=10.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
apscheduler>=3.10.4
python-multipart>=0.0.9
google-api-python-client>=2.150.0
google-auth-oauthlib>=1.2.1
google-auth-httplib2>=0.2.0

# Pi / optional — install on device as needed:
# picamera2  # Raspberry Pi OS only
# luma.oled / luma.lcd  # SPI display drivers
# sounddevice  # microphone capture
# numpy  # often pulled by audio stacks
